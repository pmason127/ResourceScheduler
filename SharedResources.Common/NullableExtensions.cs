using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SharedResources.Common
{
    public static class NullableExtensions
    {
        //public static T GetValueOrDefault<T>(this Nullable<T> val, T defaulValue) where T:struct
        //{
        //    if (!val.HasValue)
        //        return defaulValue;

        //    return val.Value;
        //}
    }
}
